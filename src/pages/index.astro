---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Analytics from "@vercel/analytics/astro";
import StoreList from "../components/StoreList.astro"; // StoreListコンポーネントをインポート
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OKIPOKA | 沖縄ポーカー情報ポータル</title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <style>
      .hero {
        position: relative;
        text-align: center;
        padding: 6rem 1rem;
        color: #fff;
        background-image: url('/images/hero-background.jpg'); /* お好みの背景画像を指定してください */
        background-size: cover;
        background-position: center;
        border-bottom: 1px solid var(--color-border);
      }
      .hero::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        z-index: 1;
      }
      .hero h1, .hero p {
        position: relative;
        z-index: 2;
        text-shadow: 0 2px 4px rgba(0,0,0,0.5);
      }
      .hero h1 { font-size: 3rem; }
      .hero p { font-size: 1.2rem; color: #f0f0f0; }
      .section-title {
        text-align: center;
        margin-bottom: 2rem;
        font-size: 2.2rem;
        color: #333;
      }
      #today-all-card-wrapper {
        max-height: 400px;
        overflow-y: auto;
        padding: 1rem;
        background: #fff;
        border-radius: 8px;
        border: 1px solid var(--color-border);
      }
      #today-all-card {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
      }
      .info-card {
        background: var(--color-surface);
        padding: 1.5rem;
        border-radius: 8px;
        border-left: 4px solid var(--color-primary);
        transition: background-color 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 5px rgba(0,0,0,0.05);
      }
      .info-card:hover {
        background-color: #fdfdfd;
        box-shadow: 0 4px 10px rgba(0,0,0,0.08);
      }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <section class="hero">
        <h1>沖縄のポーカー情報を、すべてここに。</h1>
        <p>トーナメントや店舗の最新情報をリアルタイムでお届けします。</p>
      </section>

      <div class="container">
        <section id="tournaments-today">
          <h2 class="section-title">本日のトーナメント</h2>
          <div id="today-all-card-wrapper">
            <div id="today-all-card">
              </div>
          </div>
          <div style="text-align: center; margin-top: 2rem;">
            <a href="/tournaments/tournaments" class="button">全てのトーナメントを見る</a>
          </div>
        </section>

        <section id="stores-list" style="margin-top: 4rem;">
          <h2 class="section-title">店舗一覧</h2>
          
          <StoreList />
        </section>
      </div>
    </main>
    <Footer />
    <Analytics />
    <script src="/js/header.js" type="module"></script>
    <script src="/js/todayAll.js" type="module"></script>
  </body>
</html>