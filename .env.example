# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Needed by server-side webhook sync
SUPABASE_SERVICE_ROLE_KEY=

# Square (server)
SQUARE_ENVIRONMENT=production
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_SUBSCRIPTION_PLAN_VARIATION_ID=

# Square (client)
NEXT_PUBLIC_SQUARE_APPLICATION_ID=
NEXT_PUBLIC_SQUARE_LOCATION_ID=

# Square webhook (optional: enable only when configured in Square dashboard)
# Signature key from Square webhook settings
SQUARE_WEBHOOK_SIGNATURE_KEY=
# Exact notification URL registered in Square (must match exactly)
SQUARE_WEBHOOK_NOTIFICATION_URL=

# App
# Admin email (used for limited debug/admin routes)
OKIPOKA_ADMIN_EMAIL=okipoka.jp@gmail.com
# 認証リダイレクトの origin がリクエストから解決できない場合のフォールバック先
# 通常はリクエストの origin を使うので設定不要。本番のみ明示したい場合に利用。
NEXT_PUBLIC_SITE_URL=https://okipoka.com
